# Shahrier's Portfolio

The portfolio website of Shahrier, showcasing projects, skills, and contact information.

Good, now in here please increase the size of the images other than the shahrier image and then put top 3 images then other images top left and top right side

```powershell
# Delete package-lock.json if it exists
Remove-Item package-lock.json -ErrorAction SilentlyContinue

# Clear npm cache
npm cache clean --force

# Reinstall dependencies
npm install

# Now try adding the button component again
npx shadcn@latest add button
```
